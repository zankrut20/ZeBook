% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_internal.r
\name{.apply_simule}
\alias{.apply_simule}
\title{Apply a scalar-output model function over rows of a parameter matrix.}
\usage{
.apply_simule(X, fn, output_name = "Y", all = FALSE)
}
\arguments{
\item{X}{A matrix or data frame; each row is one parameter vector.}

\item{fn}{A function of one argument (a row of `X`) that returns a
single numeric scalar.}

\item{output_name}{Character string used as the column name when `all=TRUE`.}

\item{all}{Logical; if `TRUE` return `X` augmented with an output
column named `output_name`.}
}
\value{
A numeric matrix.
}
\description{
All `*.simule()` wrappers follow the pattern:
```
  Y <- apply(X, 1, fn)
  if (all) Y <- cbind(X, <name> = Y)
  return(as.matrix(Y))
```
This helper encapsulates that pattern.
}
\keyword{internal}
